<div class="rounded-lg bg-gray-200 p-2 flex flex-col gap-2">
    <p class="text-base text-black"><%= comment.body %></p>
    <!-- User information -->
    <div class="flex items-center gap-2">
        <span class="text-sm text-slate-800">Commented by <%= comment.user.username %></span>
        <span class="text-sm text-slate-600"><%= time_ago_in_words comment.created_at %> ago </span>
    </div>
</div>