<div class="container mx-auto px-4 py-8">

  <h1 class="text-3xl font-bold mb-4 text-slate-900 dark:text-white"><%= t("menu.network") %></h1>
  <p class="text-sm mb-6 text-slate-600 dark:text-slate-300">Find people to connect with</p>

  <div class="users-grid mt-6">
    <% @users.each do |user| %>
      <%= render "users/user_card", user: user %>
    <% end %>
  </div>
</div>